[package]
name = "soma"
version = "0.3.0"
edition = "2021"
description = "Executable SOMA abstract-machine semantics and reference backends"
license = "MIT"

[features]
default = []
metal = ["dep:metal"]

[target.'cfg(target_os = "macos")'.dependencies]
metal = { version = "0.33.0", optional = true }

[profile.release]
debug = true
